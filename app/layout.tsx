import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "ShareBite — Food Rescue Platform",
  description:
    "Connecting food donors with NGOs to rescue surplus food, reduce waste, and fight hunger. UN SDG 2: Zero Hunger.",
  keywords: ["food rescue", "zero hunger", "SDG 2", "food donation", "NGO"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
